<table class="table">
    <thead>
        <tr>
            <th scope="col">Producto</th>
            <th scope="col">Precio</th>
            <th scope="col">Imagen</th>
        </tr>
    </thead>
    <tbody>
        <% if (datos.length === 0) { %>
            <tr>
                <td colspan="3">No hay datos</td>
            </tr>
        <% }else{ %>
        
        <% for (let i = 0; i < datos.length; i++) { %>
            <tr>
                <td><%= datos[i].title %></td>
                <td><%= datos[i].price %></td>
                <td><%= datos[i].thumbnail %></td>
            </tr>
        <% } } %>
    </tbody>
</table>